name: CI/CD Pipeline for Subscription Service

on:
  pull_request:
    paths:
      - 'services/subscription-service/**'
      - 'pom.xml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Make mvnw executable
        run: chmod +x ./mvnw

      - name: Build & Test
        working-directory: services/subscription-service
        run: ../mvnw -B clean verify

      - name: Upload JUnit reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: subscription-service-test-reports
          path: |
            services/subscription-service/**/target/surefire-reports/*
            services/subscription-service/**/target/failsafe-reports/*
          if-no-files-found: ignore
